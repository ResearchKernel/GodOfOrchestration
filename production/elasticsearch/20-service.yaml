---
apiVersion: v1
kind: Service
metadata:
  namespace: researchkernel
  name: elasticsearch
  labels:
    env: production
spec:
  type: ClusterIP
  selector:
    app: elasticsearch-client
  ports:
  - name: http
    port: 9200
    protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  namespace: researchkernel
  name: elasticsearch-data
  labels:
    env: production
spec:
  clusterIP: None
  selector:
    app: elasticsearch-data
  ports:
  - port: 9300
    name: transport
---
apiVersion: v1
kind: Service
metadata:
  namespace: researchkernel
  name: elasticsearch-discovery
  labels:
    env: production
spec:
  selector:
    app: elasticsearch-master
  ports:
  - name: transport
    port: 9300
    protocol: TCP